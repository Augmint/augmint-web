describe("Reserve sales", function() {
    // beforeEach(function() {
    //     cy.getUserAEurBalance().as("startingAeurBalance");
    //     cy.getUserEthBalance().as("startingEthBalance");
    // });

    it("Should buy A-EUR from reserve");
    it("Should sell A-EUR to reserve");
});
